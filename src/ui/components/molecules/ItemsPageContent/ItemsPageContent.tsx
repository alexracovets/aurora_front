"use client";

import { Result } from "@payload-types";

export const ItemsPageContent = ({ content }: { content: Result }) => {
    console.log(content);
    return (
        <div>
            
        </div>
    )
}