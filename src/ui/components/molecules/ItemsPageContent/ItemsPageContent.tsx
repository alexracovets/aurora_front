"use client";

import { Result } from "@payload-types";

export const ItemsPageContent = ({ content }: { content: Result['content'] }) => {
 
    return (
        <div>
            
        </div>
    )
}